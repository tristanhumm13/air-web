@using AirWeb.Domain.Core.Data.DataProcessing
@model string?
@{
}
@if (string.IsNullOrEmpty(Model))
{
    <span class="text-body-secondary fw-lighter">@DefaultText.PlaceholderNotEntered</span>
}
else
{
    @if (Regexes.EmailRegex.IsMatch(Model))
    {
        <a href="mailto:@Model">@Model</a>
    }
    else
    {
        @Model
    }
}
