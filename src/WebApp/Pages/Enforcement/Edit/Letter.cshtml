@page "{id:Guid?}"
@using AirWeb.WebApp.Pages.Shared
@using AirWeb.WebApp.Pages.Shared.DisplayTemplates
@using AirWeb.WebApp.Pages.Shared.EditorTemplates
@model LetterEditModel
@{
    var caseFile = Model.CaseFile!;
    ViewData["Title"] = $"Edit {Model.ItemName}";
    ViewData["NoAnchors"] = "True";
}

<h1>@ViewData["Title"]</h1>

<div class="p-3 border border-primary-subtle rounded-3 bg-light-subtle mb-3">
    <form method="post">
        <div asp-validation-summary="All" class="alert alert-danger d-flex align-items-center" role="alert">
            <partial name="Shared/_AppIconsPartial" model="@AppIcon.WarningIcon" view-data="new ViewDataDictionary(ViewData)"
                     view-data-CssClass="@("me-2")" />
        </div>
        <dl class="row">
            <dt class="col-sm-4 col-lg-3">Enforcement Case #</dt>
            <dd class="col-sm-8 col-lg-9">@caseFile.Id</dd>
            <dt class="col-sm-4 col-lg-3">Facility</dt>
            <dd class="col-sm-8 col-lg-9">
                <span class="text-body-secondary">@caseFile.FacilityId –</span> @caseFile.FacilityName
            </dd>
            <dt class="col-sm-4 col-lg-3">@Html.DisplayNameFor(_ => caseFile.DiscoveryDate)</dt>
            <dd class="col-sm-8 col-lg-9">@Html.DisplayFor(_ => caseFile.DiscoveryDate, DisplayTemplate.ShortDateOnlyNullable)</dd>
        </dl>

        <h2 class="h4">Update Details</h2>

        <div class="mb-3">
            @Html.EditorFor(m => m.Item.Notes, EditorTemplate.Textarea, additionalViewData: new { Rows = 5 })
        </div>

        @if (Model.ShowIssueDate)
        {
            <div class="mb-3">
                @Html.EditorFor(model => model.Item.IssueDate, EditorTemplate.InputDateOnlyNullable)
            </div>
        }

        @if (Model.ShowResponseRequested || Model.ShowResponse)
        {
            <h3 class="h5">Response Details</h3>
        }

        @if (Model.ShowResponseRequested)
        {
            <div class="mb-3">
                @Html.EditorFor(m => m.Item.ResponseRequested, EditorTemplate.Checkbox)
            </div>
        }

        @if (Model.ShowResponse)
        {
            <div class="mb-3">
                @Html.EditorFor(m => m.Item.IsResponseReceived, EditorTemplate.Checkbox)
            </div>
            <div class="@(Model.Item.IsResponseReceived ? "" : "d-none")" id="responseDetails">
                <div class="mb-3">
                    @Html.EditorFor(model => model.Item.ResponseReceived, EditorTemplate.InputDateOnlyNullable)
                </div>
                <div class="mb-3">
                    @Html.EditorFor(m => m.Item.ResponseComment, EditorTemplate.Textarea, additionalViewData: new { Rows = 4 })
                </div>
            </div>

            @section Scripts {
                <script src="~/js/compliance/responseDetailsSection.js"></script>
            }
        }

        <partial name="Shared/_SubmitCancelButtons" />
    </form>
</div>
