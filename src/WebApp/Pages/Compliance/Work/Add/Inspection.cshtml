@page "{facilityId?}"
@using AirWeb.Domain.Compliance.ComplianceEntities.ComplianceMonitoring
@using AirWeb.WebApp.Pages.Shared
@using AirWeb.WebApp.Pages.Shared.EditorTemplates
@model InspectionAddModel
@{
    ViewData["Title"] = $"Add a new {Model.WorkType.GetDisplayName()}";
    ViewData["NoAnchors"] = "True";
}

<h1>@ViewData["Title"]</h1>
<p class="lead">For Facility:
    <partial name="Shared/FacilityPartials/_FacilityAirsName" model="Model.Facility" />
</p>
@if (Model.CaseFile is not null)
{
    <div class="p-3 border border-info rounded-3 bg-info-subtle mb-3">
        This @Model.WorkType.GetDisplayName() will be linked to Enforcement Case #@Model.CaseFileId.
        <a asp-route-facilityId="@Model.FacilityId" class="btn btn-outline-primary btn-sm ms-2 align-baseline">Continue without linking</a>
    </div>
}

<div class="p-3 border border-primary-subtle rounded-3 bg-light-subtle">
    <form method="post">
        <div asp-validation-summary="All" class="alert alert-danger d-flex align-items-center" role="alert">
            <partial name="Shared/_AppIconsPartial" model="@AppIcon.WarningIcon" view-data="new ViewDataDictionary(ViewData)"
                     view-data-CssClass="@("me-2")" />
        </div>

        <div class="mb-3">
            @Html.EditorFor(m => m.Item.ResponsibleStaffId, EditorTemplate.Select,
                additionalViewData: new { Items = Model.StaffSelectList, Placeholder = DefaultText.SelectOptionRequired })
        </div>

        <div class="container">
            <fieldset class="row mb-3 border rounded-3 pt-2">
                <div class="col">
                    <div class="mb-3 d-flex">
                        <legend class="h6 float-none w-auto me-4">Inspection Date</legend>
                        <div class="form-check form-switch">
                            <input asp-for="Item.MultiDayInspection" class="form-check-input" switch>
                            <label asp-for="Item.MultiDayInspection" class="form-check-label"></label>
                        </div>
                    </div>
                    <div class="row">
                        <div class="input-group mb-3 @(Model.Item.MultiDayInspection ? "col-md" : "")" id="date-multi-off">
                            @Html.EditorFor(m => m.Item.InspectionStartedDate, EditorTemplate.InputDateOnly)
                            @Html.EditorFor(m => m.Item.InspectionStartedTime, EditorTemplate.InputTimeOnly)
                            @if (!Model.Item.MultiDayInspection)
                            {
                                @Html.EditorFor(m => m.Item.InspectionEndedTime, EditorTemplate.InputTimeOnly)
                            }
                        </div>
                        <div class="col-md input-group mb-3 @(Model.Item.MultiDayInspection ? "" : "d-none")" id="date-multi-on">
                            @Html.EditorFor(m => m.Item.InspectionEndedDate, EditorTemplate.InputDateOnly)
                            @if (Model.Item.MultiDayInspection)
                            {
                                @Html.EditorFor(m => m.Item.InspectionEndedTime, EditorTemplate.InputTimeOnly)
                            }
                        </div>
                    </div>
                </div>
            </fieldset>
        </div>

        <div class="row">
            <div class="col-12 col-lg mb-3">
                @Html.EditorFor(m => m.Item.InspectionReason, EditorTemplate.SelectNoPlaceholder,
                    additionalViewData: new { Items = Html.GetEnumSelectList<InspectionReason>() })
            </div>

            <div class="col-md mb-3">
                @Html.EditorFor(m => m.Item.WeatherConditions, EditorTemplate.InputNullable)
            </div>
            <div class="col-md mb-3">
                @Html.EditorFor(m => m.Item.InspectionGuide, EditorTemplate.InputNullable)
            </div>
        </div>

        <div class="container">
            <fieldset class="row mb-3 border rounded-3 pt-2">
                <div class="col">
                    <legend class="h6 mb-3">
                        <partial name="Shared/_RequiredLabelPartial" />
                        Inspection Analysis
                    </legend>
                    <div class="row">
                        <div class="col-md mb-3">
                            @Html.EditorFor(m => m.Item.FacilityOperating, EditorTemplate.Checkbox)
                        </div>
                        <div class="col-md mb-3">
                            @Html.EditorFor(m => m.Item.DeviationsNoted, EditorTemplate.Checkbox)
                        </div>
                    </div>
                </div>
            </fieldset>
        </div>

        <div class="row">
            <div class="mb-3">
                @Html.EditorFor(m => m.Item.Notes, EditorTemplate.Textarea, additionalViewData: new { Rows = 5 })
            </div>
        </div>

        <div class="row">
            <div class="col-md-6 mb-3">
                @Html.EditorFor(m => m.Item.AcknowledgmentLetterDate, EditorTemplate.InputDateOnlyNullable)
            </div>
            <div class="col-md mb-3">
                @Html.EditorFor(m => m.Item.FollowupTaken, EditorTemplate.Checkbox)
            </div>
        </div>

        <partial name="Shared/_SubmitCancelButtons" />
        <input asp-for="Item.CaseFileId" type="hidden" />
        <input asp-for="Item.IsRmpInspection" type="hidden" />
    </form>
</div>

@section Scripts {
    <script src="~/js/compliance/setMultidayEntry.js"></script>
}
